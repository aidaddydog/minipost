# 应用/主题/端口
APP_HOST=0.0.0.0
APP_PORT=8000
THEME_NAME=default

# 数据库（强制 Postgres）
DB=postgres
PG_HOST=postgres
PG_PORT=5432
PG_DB=minipost
PG_USER=minipost
PG_PASSWORD= # 部署脚本会自动注入强口令

# 导航与安全
USE_REAL_NAV=false
UFW_OPEN=true
JWT_SECRET= # 部署脚本会自动注入
