[Unit]
Description=minipost FastAPI service
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/minipost
ExecStart=/opt/minipost/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=3
User=root

[Install]
WantedBy=multi-user.target
